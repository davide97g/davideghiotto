# Local: point at the ral-gate service (see services/ral-gate).
# Leave unset / empty to keep figures permanently locked (fail-closed).
VITE_RAL_API_URL=http://127.0.0.1:8787

# Google Analytics 4 measurement ID. Leave empty to disable gtag entirely.
VITE_GA_MEASUREMENT_ID=G-BTP8Z49CM7

